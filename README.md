# ashejim.github.io

Personal site for Jim Ashe, PhD — served via GitHub Pages at <https://ashejim.github.io>.

A single self-contained `index.html` (inline CSS, no build step, no external dependencies).
Edit `index.html` and push to `main`; GitHub Pages redeploys automatically.

The `eval-benchmark` project card is present but commented out in `index.html` —
uncomment it once that repo is published.
